 using System.Linq;
 using AllConsulting.Entities;

namespace AllConsulting.Web.Dto
{
    public static class ExtensionMethod
    {
        //public static IQueryable<Order> FilterDelevery(this IQueryable<Order> orders, string strSearch)
        //{
        //    foreach (var order in orders)
        //    {
        //        if (order.DeliveryDate.HasValue)
        //        {
        //            if (order.DeliveryDate.Value.ToString("dd/MM/yyyy").Contains(strSearch))
        //            {
        //                yield return order;
        //            }
        //        }
        //    }
        //    yield return ;
        //}
    }
}